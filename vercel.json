{
  "buildCommand": "npm install && npm run build",
  "installCommand": "npm install && composer install --no-dev --no-interaction --prefer-dist",
  "outputDirectory": "public",
  "framework": null,
  "functions": {
    "api/**/*.php": {
      "runtime": "vercel-php@0.9.0",
      "includeFiles": "**"
    }
  },
  "routes": [
    {
      "src": "^/build/(.*)",
      "headers": { "Cache-Control": "public, max-age=31536000, immutable" }
    },
    {
      "src": "^/(.*)",
      "dest": "api/index.php"
    }
  ],
  "crons": []
}
